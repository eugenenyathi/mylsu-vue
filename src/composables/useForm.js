const useForm = () => {
	const updateStNumber = (studentNumber) => {};

	const togglePassword = (showPassword, pwdType) => {};

	return {
		updateStNumber,
		togglePassword,
	};
};

export default useForm;
